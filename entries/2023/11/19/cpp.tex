% % % % mainfile: ../../../../master.tex
% \subsection{Introduction to C++ Classes}
% \label{task:20231120_cpp}

% Every class defines a type. The typename is the same as the name of the class. Hence, our \path{Sales_item} class defines a type named \path{Sales_item}, and as with the built-in types, we can define a variable of a class type:
% \begin{lstlisting}
% Sales_item item;
% \end{lstlisting}
